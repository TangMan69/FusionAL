{
  "name": "commander-mcp-server",
  "version": "1.0.0",
  "description": "Self-hosted Desktop-Commander-style MCP server (filesystem + terminal + process control) built to run inside the FusionAL gateway stack.",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsc --watch"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "express": "^5.2.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^22.10.0",
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=18"
  }
}
